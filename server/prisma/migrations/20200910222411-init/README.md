# Migration `20200910222411-init`

This migration has been generated by lorenries at 9/10/2020, 6:24:11 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."StreamInfo" (
"id" SERIAL,
"name" text   NOT NULL ,
"userId" integer   NOT NULL ,
"streamId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Stream" (
"id" SERIAL,
"url" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."User" (
"id" SERIAL,
"username" text   NOT NULL ,
"password" text   NOT NULL ,
"role" "Role"  NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."_StreamToUser" (
"A" integer   NOT NULL ,
"B" integer   NOT NULL 
)

CREATE UNIQUE INDEX "Stream.url_unique" ON "public"."Stream"("url")

CREATE UNIQUE INDEX "User.username_unique" ON "public"."User"("username")

CREATE UNIQUE INDEX "_StreamToUser_AB_unique" ON "public"."_StreamToUser"("A", "B")

CREATE INDEX "_StreamToUser_B_index" ON "public"."_StreamToUser"("B")

ALTER TABLE "public"."StreamInfo" ADD FOREIGN KEY ("streamId")REFERENCES "public"."Stream"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."StreamInfo" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_StreamToUser" ADD FOREIGN KEY ("A")REFERENCES "public"."Stream"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_StreamToUser" ADD FOREIGN KEY ("B")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200910222411-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,41 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model StreamInfo {
+  id       Int    @default(autoincrement()) @id
+  name     String
+  stream   Stream @relation(fields: [streamId], references: [id])
+  user     User   @relation(fields: [userId], references: [id])
+  userId   Int
+  streamId Int
+}
+
+model Stream {
+  id         Int          @default(autoincrement()) @id
+  url        String       @unique
+  users      User[]
+  streamInfo StreamInfo[]
+}
+
+enum Role {
+  USER
+  ADMIN
+}
+
+model User {
+  id         Int          @default(autoincrement()) @id
+  username   String       @unique
+  password   String
+  role       Role
+  streams    Stream[]
+  streamInfo StreamInfo[]
+}
```


